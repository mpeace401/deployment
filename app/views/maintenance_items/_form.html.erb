<%= form_with(model: maintenance_item) do |form| %>
  <% if maintenance_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(maintenance_item.errors.count, "error") %> prohibited this maintenance_item from being saved:</h2>

      <ul>
        <% maintenance_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :item_name, style: "display: block" %>
    <%= form.text_field :item_name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.label :total_amount, style: "display: block" %>
    <%= form.number_field :total_amount %>
  </div>

  <div>
    <%= form.label :available_amount, style: "display: block" %>
    <%= form.number_field :available_amount %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
